const initialState = {
  isAuthenticated: false,
  user: {},
  errors: {},
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
